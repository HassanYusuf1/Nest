@model BilderViewModel



<div class="row row-cols-1 row-cols-md-3 g-4">
    @foreach (var bilde in Model.Bilder)
    {
        <partial name="_BildeCard" model="bilde" />
    }
</div>

<div class="mt-4">
    <a href="@Url.Action("Create", "Bilde")" class="btn upload-btn">Upload New Image</a>
</div>